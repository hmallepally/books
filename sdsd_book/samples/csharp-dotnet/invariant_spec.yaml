version: "1.0"
spec_type: "invariant"
feature_name: "AuthService"

boundaries:
  inputs:
    - name: tenantId
      type: string
    - name: username
      type: string
    - name: password
      type: string

invariants:
  - id: INV-001
    description: "Tenant Isolation: All authentication attempts must explicitly provide a valid tenantId."
    enforcement_level: "FATAL"
