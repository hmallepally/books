```python
stack = []
stack.append(10)           # Push — O(1)
stack.append(20)
stack.append(30)
stack[-1]                  # View top (returns 30) — O(1)
stack.pop()                # Pop (removes 30) — O(1)
```
